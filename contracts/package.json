
{
  "name": "flashloan-contracts",
  "version": "1.0.0",
  "description": "Flash loan arbitrage smart contracts for Base network",
  "scripts": {
    "deploy": "node deploy.js",
    "compile": "npx hardhat compile",
    "verify": "npx hardhat verify --network base"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "ethers": "^6.7.1",
    "hardhat": "^2.17.2"
  }
}
